## 2026-06-01 - US-011
- Added route GET `/stats` to `web/app.py`: iterates `["sezioni_cai", "enti", "allegati", "bilanci"]`, uses `_table_exists()` guard before COUNT(*), passes `counts` dict + `active_page='stats'`
- Created `web/templates/stats.html`: title "Statistiche", subtitle "In costruzione", 4 Bootstrap cards in a responsive row grid (col-sm-6 col-lg-3) with colored display-6 numbers
- Files changed: `web/app.py`, `web/templates/stats.html` (new)
- **Learnings for future iterations:**
  - Iterating over a list of table names and using `f"SELECT COUNT(*) FROM {table}"` avoids repeating boilerplate; `_table_exists()` guard makes it safe for empty DBs
  - `counts` dict is passed as a single Jinja2 var and accessed as `counts.sezioni_cai` etc.
  - Manual browser verification needed (no browser tools)
---

## 2026-06-01 - US-010
- Added route GET `/gruppi-regionali` to `web/app.py`: SELECT with `json_extract(gr_indirizzo_sede, '$.province')` + LEFT JOIN enti; `active_page='gruppi-regionali'`
- Created `web/templates/gruppi_regionali.html` with 6-column table (Nome GR, Provincia, Email, Telefono, Sito web, Ente RUNTS)
- Rows with `gr_id_runts IS NULL` have `class="table-warning"`; badge `bg-warning text-dark` for non-agganciati
- Empty-table message with `python3 -m scraper.cai_main` instruction
- Files changed: `web/app.py`, `web/templates/gruppi_regionali.html` (new)
- **Learnings for future iterations:**
  - Province extracted via `json_extract(g.gr_indirizzo_sede, '$.province')` in SQL — consistent with `json_extract(s.cai_indirizzo_sede, '$.city')` pattern in sections list
  - `empty_ctx` dict defined at top of route avoids repeating the dict in multiple early-return branches
  - Manual browser verification needed (no browser tools)
---

## 2026-06-01 - US-009
- Added `--no-groups` flag to `scraper/cai_main.py` argparse (default: GR fetch is enabled)
- Added `_norm_name(s)` helper: lowercase, strip accents (NFD/Mn), remove non-alphanumeric, collapse whitespace
- Added `_match_gr_to_enti(conn, gr)`: (1) exact CF match via SQL, (2) fuzzy SequenceMatcher ratio >= 0.5 over all enti.denominazione
- GR block: fetch → match → set gr_id_runts → upsert; tracks scaricati/inseriti/aggiornati/agganciati
- Report section printed only when `--no-groups` is not set
- Files changed: `scraper/cai_main.py`
- **Learnings for future iterations:**
  - `difflib.SequenceMatcher(None, a, b).ratio()` is the standard fuzzy match; threshold 0.5 is permissive for short names
  - Fuzzy match scans ALL enti rows (N=226 max) — acceptable for this scale, no index needed
  - CF exact match takes priority; fuzzy only runs when `gr_codice_fiscale` is None
  - `_norm_name` uses `unicodedata.normalize("NFD")` + Mn category filter for accent stripping (pure stdlib)
---

## 2026-06-01 - US-008
- Added `fetch_regional_groups() -> list[dict]` to `scraper/cai_scraper.py`
- Calls GET `https://www.cai.it/wp-json/cai-section/v2/regional-groups-list` with Origin/Referer headers
- Normalizes API fields to `gr_` prefixed column names; serializes officeAddress/postalAddress as json.dumps()
- Uses existing `_with_retry()` for max 3 retries with exponential backoff
- Files changed: `scraper/cai_scraper.py`
- **Learnings for future iterations:**
  - Added `_REGIONAL_GROUPS_URL` constant alongside existing URL constants
  - Pattern is identical to `fetch_all_sections()`: client with headers, `_with_retry()`, isinstance check, list comprehension
  - `gr_id_runts` is NOT set here — it's populated by the matching logic in cai_main.py (US-009)
---

## 2026-06-01 - US-007
- Added `CREATE TABLE IF NOT EXISTS gruppi_regionali_cai` to SCHEMA in `scraper/db.py` with 16 columns (gr_codice PRIMARY KEY + 15 fields)
- Added `CREATE INDEX IF NOT EXISTS idx_gr_cai_cf ON gruppi_regionali_cai(gr_codice_fiscale)` to SCHEMA
- Added migration entries to `_MIGRATIONS` for both the table DDL and the index
- Implemented `upsert_gruppo_regionale(conn, data)` following the same INSERT OR REPLACE pattern as `upsert_sezione_cai()`
- Files changed: `scraper/db.py`
- **Learnings for future iterations:**
  - New tables follow the pattern: add DDL to SCHEMA (for fresh DBs) + add same DDL to _MIGRATIONS (for existing DBs) + add index to both
  - `upsert_gruppo_regionale` auto-sets `gr_scraped_at` to now if not provided (same as `cai_scraped_at` pattern)
  - `gr_id_runts` is nullable — it gets populated by cai_main.py matching logic (US-009)
---

## 2026-06-01 - US-012 (ralph/stats branch)
- Added D3.js 7 CDN `<script src="https://cdn.jsdelivr.net/npm/d3@7">` to `{% block head %}` in stats.html
- Added `<div id="map-regioni" style="height:420px">` section in stats.html
- D3 choropleth: loads `/static/it-regions.geojson`, `d3.geoMercator()` centered at [12.5, 42] (Italy), `d3.scaleSequential(d3.interpolateBlues)` scaled 0→max(n_sezioni)
- NORM dict maps GeoJSON names to DB uppercase names (Valle d'Aosta, Trentino-Alto Adige, Friuli Venezia Giulia, Emilia-Romagna are the 4 cases needing normalization)
- Tooltip on hover: div appended to body, shown on mouseover/mousemove, hidden on mouseout
- Regions with no DB data colored `#eee`
- Files changed: `web/templates/stats.html`
- **Learnings for future iterations:**
  - geoBoundaries ADM2 uses plain Italian names (NOT NUTS bilingual); only 4 regions need normalization to match DB uppercase
  - D3 tooltip must be appended to `body` (not the SVG container) to avoid clipping
  - `d3.json()` returns a promise — chain with `.then()`; no async/await in plain script blocks
  - Manual browser verification needed (no browser tools)
---

## 2026-06-01 - US-011 (ralph/stats branch)
- Added qualità dati table to stats.html: 4 rows (sezioni no CF, ETS non agganciati, sezioni no soci, GR non agganciati)
- Badge bg-warning if n>0, bg-success if n=0; "Vedi →" link column
- Data from `qualitaDati` JS var (set in {% block head %})
- Files changed: `web/templates/stats.html`
---

## 2026-06-01 - US-010 (ralph/stats branch)
- Added top 10 sottosezioni table to stats.html: Pos, Denominazione (link ?tab=sottosezioni), N, progress bar (max=30 Bergamo)
- Data from `top10Sottosezioni` JS var
- Files changed: `web/templates/stats.html`
---

## 2026-06-01 - US-009 (ralph/stats branch)
- Added donut chart `chart-allegati-tipo` to stats.html using Chart.js doughnut type
- 8 Bootstrap-palette colors; legend on right showing tipo + count
- Data from `allegatiPerTipo` JS var
- Files changed: `web/templates/stats.html`
---

## 2026-06-01 - US-008 (ralph/stats branch)
- Added line chart with entity selector to stats.html
- `<select id="sel-ente">` populated from `bilanciEnte`; updates chart on change without page reload
- Line chart with fill and tension; tooltip formats as €
- Files changed: `web/templates/stats.html`
---

## 2026-06-01 - US-007 (ralph/stats branch)
- Added vertical bar chart `chart-proventi-regione` to stats.html (Chart.js vertical bar, color rgba(25,135,84,0.7))
- Y-axis tick formatted as €Xk/€XM; tooltip formats full euro amount
- Note: "Dati parziali — solo enti con bilancio 2024 analizzato"
- Files changed: `web/templates/stats.html`
---

## 2026-06-01 - US-006 (ralph/stats branch)
- Added copertura ETS progress bars section to stats.html (4 items, dynamic color bg-success/warning/danger)
- Data from `copertura` JS var; color threshold: >=80% success, 20-79% warning, <20% danger
- Files changed: `web/templates/stats.html`
---

## 2026-06-01 - US-005 (ralph/stats branch)
- Added top 10 soci table to stats.html: Bootstrap table-sm with Pos, Denominazione (link), Soci, progress bar
- Progress bar width = soci/28992*100 (SAT max); data from `top10Soci` JS var
- Files changed: `web/templates/stats.html`
---

## 2026-06-01 - US-004 (ralph/stats branch)
- Added Chart.js CDN to `{% block head %}` in stats.html
- Added horizontal bar chart `chart-soci-regione`: indexAxis='y', data sorted DESC, rgba(13,110,253,0.7)
- Tooltip: 'X soci — Y sezioni'
- Files changed: `web/templates/stats.html`
---

## 2026-06-01 - US-003 (ralph/stats branch)
- Downloaded Italian regions GeoJSON from geoBoundaries API (openpolis URL in PRD returns 404 — repo does not exist)
- Used geoboundaries.org API: `GET /api/current/gbOpen/ITA/ADM2/` → `gjDownloadURL` → ADM2 = 20 Italian regions
- Applied Douglas-Peucker simplification (epsilon=0.01 ≈ 1km) in pure Python to reduce from ~10MB to 396KB
- Feature properties: `{name: "Piemonte", iso: "ITA-1234"}` — name is plain Italian (no bilingual NUTS names)
- StaticFiles mount already present at `web/app.py:17`; `web/static/` directory already existed
- Files changed: `web/static/it-regions.geojson` (new, 396KB)
- **Learnings for future iterations:**
  - openpolis/geoboundaries-ita GitHub repo does NOT exist — use geoboundaries.org API instead
  - geoboundaries.org ADM2 = Italian 20 regions (ADM1 = 5 macro-areas)
  - GeoJSON property `name` contains plain Italian names (e.g., "Trentino-Alto Adige", NOT the NUTS bilingual form "Trentino-Alto Adige/Südtirol") — US-012 normalization dict must account for this
  - Manual browser verification needed (no browser tools)
---

## 2026-06-01 - US-002 (ralph/stats branch)
- Extended `/stats` route in `web/app.py`: computes 8 aggregations (soci_per_regione, proventi_2024_per_regione, top10_soci, top10_sottosezioni, allegati_per_tipo, bilanci_per_ente, copertura_ets, qualita_dati)
- `bilanci_per_ente` built via dict grouping — includes only enti with >= 2 anni di totale_proventi non null
- `qualita_dati` has fixed 4-item order regardless of which tables exist (defaults to 0 when table missing)
- `stats.html` updated: added `{% block head %}` with `<script>` defining JS vars via `| tojson` (sociRegione, proventi2024Regione, top10Soci, top10Sottosezioni, allegatiPerTipo, bilanciEnte, copertura, qualitaDati)
- Files changed: `web/app.py`, `web/templates/stats.html`
- **Learnings for future iterations:**
  - `has_sezioni / has_enti / has_bilanci` boolean flags computed once at the top of the try block — avoids repeated `_table_exists()` calls
  - `bilanci_per_ente` uses a plain dict `_ente_map` keyed by id_runts — no collections import needed
  - `copertura_ets["totale"]` updated to actual `kpi["enti_ets"]` when enti table exists, fallback 226
  - Manual browser verification needed (no browser tools)
---

## Codebase Patterns
- Pre-commit hook runs 6 pytest tests (`scraper/test_sede_legale.py`, `scraper/test_upsert.py`) — all commits must pass them
- FastAPI app is in `web/app.py`; Jinja2 templates in `web/templates/`; static assets in `web/static/`
- No mypy installed in the venv; typecheck = syntax OK + pytest passing
- `_tr(request, template, context)` is the helper to render templates — always pass the context dict as the third argument
- SQLite DB accessed read-only via `get_db()`; tables checked for existence with `_table_exists()` before querying
- `base.html` includes Bootstrap 5 CSS and (since US-003) Bootstrap 5 JS bundle — no need to add it in individual templates
- The `active_page` context var is used in base.html for nav-link active states (set by each route in app.py)

# Ralph Progress Log
Started: Lun  1 Giu 2026
---

## 2026-06-01 - US-001
- Added `sticky-top` class to `<nav class="navbar">` in `web/templates/base.html`
- Header now stays visible during scroll on all pages
- Files changed: `web/templates/base.html`
- **Learnings for future iterations:**
  - Bootstrap 5 `sticky-top` on the `<nav>` element is sufficient — no CSS override needed
  - The container already has `pb-5` so content isn't hidden behind anything at the bottom
  - Manual browser verification needed (no browser tools available in this env)
---

## 2026-06-01 - US-002
- Added `<footer class="footer fixed-bottom bg-dark text-white py-2">` to `web/templates/base.html`
- Footer shows 'Montagna Servizi S.C.P.A. — Via Errico Petrella 19, 20124 Milano — montagnaservizi.com'
- Container already had `pb-5` so no additional padding needed
- Files changed: `web/templates/base.html`
- **Learnings for future iterations:**
  - `fixed-bottom` footer is inside the `<body>` but outside the `.container` — place it after the closing `</div>` of the main container
  - Bootstrap `pb-5` (3rem) is enough clearance for a `py-2` footer (~2.5rem tall)
  - Manual browser verification needed (no browser tools available in this env)
---

## 2026-06-01 - US-003
- Added 4 nav-link items to navbar in `web/templates/base.html`: Sezioni (/), Gruppi Regionali (/gruppi-regionali), ETS (/ets), Statistiche (/stats)
- Active state uses `{% if active_page == '...' %}active{% endif %}` Jinja2 conditionals
- Made navbar collapsible on mobile with `navbar-expand-lg` + `navbar-toggler` button + `collapse navbar-collapse` div
- Added Bootstrap 5 JS bundle to base.html (required for the toggler; was previously absent)
- Updated all 3 `_tr()` calls in the `/` route to pass `active_page='sezioni'`
- Files changed: `web/templates/base.html`, `web/app.py`
- **Learnings for future iterations:**
  - Bootstrap JS must now be in base.html (added here for the toggler) — no need to add it in individual templates
  - Future routes (/ets, /gruppi-regionali, /stats) must pass `active_page` matching their menu key
  - Manual browser verification needed (no browser tools available in this env)
---

## 2026-06-01 - US-004
- Changed `PAGE_SIZE = 20` to `PAGE_SIZE = 50` in `web/app.py` (line 14)
- Total pages recalculate automatically via `(total + PAGE_SIZE - 1) // PAGE_SIZE`
- LIMIT/OFFSET pagination unchanged — uses `PAGE_SIZE` constant correctly throughout
- Files changed: `web/app.py`
- **Learnings for future iterations:**
  - `PAGE_SIZE` is a single constant controlling all pagination math — one-line change
  - Manual browser verification needed (no browser tools available in this env)
---

## 2026-06-01 - US-005
- Added `issues: Optional[int] = None` parameter to `enti_list` route in `web/app.py`
- When `issues=1`, appends a single OR-compound clause to the WHERE conditions covering: fuzzy_nome match note, cf_mismatch% match note, missing lat/lon, no bilanci record, no allegati record
- Also added `issues: 0` to the early-return contexts (no DB, no table)
- Added "Solo problemi dati" checkbox (name=issues value=1) next to the ETS checkbox in `web/templates/list.html`
- Propagated `issues_param` into back_url set variable and all pagination `<a>` hrefs
- Added "Filtro problemi attivo" badge in the results header when issues=1
- Files changed: `web/app.py`, `web/templates/list.html`
- **Learnings for future iterations:**
  - The issues clause uses no bound parameters (all literals) — just append the string to `clauses`, no `params` extension needed
  - `e.lat IS NULL OR e.lon IS NULL` will match all sezioni_cai with no LEFT JOIN match in enti (expected per PRD spec)
  - Both `ets_param` and `issues_param` are needed as separate Jinja2 variables in the pagination block
  - Manual browser verification needed (no browser tools available in this env)
---

## 2026-06-01 - US-006
- Added route GET /ets in `web/app.py`: SELECT enti LEFT JOIN sezioni_cai ON codice_fiscale=cai_codice_fiscale, ordered non-agganciati first
- Created `web/templates/ets.html` with table (Denominazione, Comune, Regione, Codice CAI, Status) and counter
- Status badges: bg-success 'Agganciato' (exact match), bg-warning 'CF mancante CAI' (fuzzy_nome), bg-danger 'CF discordante' (cf_mismatch%), bg-warning text-dark 'Non agganciato' (NULL codice_cai)
- Counter computed dynamically from query results (no hardcoded 226)
- Files changed: `web/app.py`, `web/templates/ets.html`
- **Learnings for future iterations:**
  - `_table_exists()` guard needed for both `enti` and `sezioni_cai` before the JOIN query
  - Jinja2 supports `.startswith()` on string values from sqlite3.Row — use `e.cai_match_note and e.cai_match_note.startswith('...')` to guard None
  - Counter can be derived from `len()` and `sum()` over the already-fetched list rather than a separate COUNT query
  - Manual browser verification needed (no browser tools available in this env)
---

## 2026-06-01 - US-001 (ralph/stats branch)
- Replaced `/stats` route in `web/app.py`: computes 7 KPIs — sezioni_totali, soci_totali (SUM), enti_ets (COUNT enti), ets_agganciati (JOIN), gr_totali (COUNT gruppi_regionali_cai), gr_agganciati (WHERE gr_id_runts IS NOT NULL), bilanci_analizzati (COUNT bilanci), copertura_bilanci_pct (COUNT DISTINCT id_runts / enti_ets * 100)
- Replaced `web/templates/stats.html`: 7 col-sm-6 col-lg cards, removed "In costruzione", badge notation X/total for ETS agganciati and GR agganciati
- Files changed: `web/app.py`, `web/templates/stats.html`
- **Learnings for future iterations:**
  - `kpi` dict (replacing old `counts`) is passed to template; each card accesses `kpi.field_name`
  - `COALESCE(SUM(...), 0)` needed for soci_totali when table is empty
  - Jinja2 number formatting: `"{:,}".format(n).replace(",", ".")` for Italian thousands separator
  - Manual browser verification needed (no browser tools)
---
