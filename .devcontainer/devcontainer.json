{
  "name": "RUNTS-CAI",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "pip install -r scraper/requirements.txt -r web/requirements.txt && pre-commit install && playwright install chromium --with-deps",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff",
        "ms-python.mypy-type-stubs"
      ]
    }
  }
}
